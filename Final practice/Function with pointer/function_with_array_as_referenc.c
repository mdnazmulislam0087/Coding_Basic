#include<stdio.h>
void func(int a[], int n)    // এরে এবং সাইজ প্যারামিটার হিসেবে নেওয়া হচ্ছে। 
{
    a[0] = 100;    // এরের প্রথম ইন্ডেক্সের ভেলু চেঞ্জ করে ১০০ করে দেওয়া হচ্ছে। 
}
int main()
{
    int a[5] = {10,20,30,40,50};
    func(a,5);           // ফাংশন কল করে এরে এবং সাইজ পাস করে দেওয়া হচ্ছে। 
    for(int i=0;i<5;i++)
    {
        printf("%d ",a[i]);   // এরে প্রিন্ট করা হচ্ছে। 
    }
}